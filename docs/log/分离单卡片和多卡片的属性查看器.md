当前界面存在信息冗余问题，卡片尺寸在统计区域、卡片列表和编辑区域重复显示，造成不必要的视觉干扰。同时，单张卡片和多张卡片的使用场景存在本质差异，应当采用不同的界面设计方案。

## 问题分析

当前属性查看器设计存在以下结构性问题：

**信息重复展示**：尺寸数据在统计摘要、详细列表和编辑区域三个位置重复出现，对于单卡片场景而言完全多余。

**界面功能混合**：单卡片编辑和多卡片批量管理属于不同的操作模式，强制使用同一界面导致功能冗余和用户体验下降。

**视觉层次混乱**：过多的信息展示区域分散了用户注意力，影响了核心编辑功能的突出性。

## 解决方案

建议创建两个专门化的Modal类来分别处理不同的使用场景：

```
// SingleCardPropertiesModal.ts - 专门用于单卡片编辑
import { Modal, Notice } from "obsidian";

export class SingleCardPropertiesModal extends Modal {
  constructor(app, card, cardService) {
    super(app);
    this.card = card;
    this.cardService = cardService;
    this.cardData = card.getData();
  }

  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    
    // 简洁的标题
    contentEl.createEl("h2", { text: "编辑卡片尺寸" });
    
    // 当前尺寸信息 - 简洁显示
    const currentInfoDiv = contentEl.createDiv({ cls: "current-card-info" });
    currentInfoDiv.innerHTML = `
      <div class="card-summary">
        <div class="size-display">
          <span class="size-label">当前尺寸：</span>
          <span class="size-value" id="current-size">${this.cardData.width} × ${this.cardData.height} px</span>
        </div>
        <div class="position-display">
          <span class="pos-label">位置：</span>
          <span class="pos-value">X: ${this.cardData.x}, Y: ${this.cardData.y}</span>
        </div>
      </div>
    `;

    // 内容预览（如果有文本）
    if (this.cardData.text) {
      const previewDiv = contentEl.createDiv({ cls: "content-preview" });
      const previewText = this.cardData.text.length > 100 
        ? this.cardData.text.substring(0, 100) + "..." 
        : this.cardData.text;
      previewDiv.innerHTML = `
        <div class="preview-label">内容预览：</div>
        <div class="preview-text">${previewText}</div>
      `;
    }

    contentEl.createEl("hr");

    // 尺寸编辑区域
    this.createDimensionEditor(contentEl);
    
    this.addStyles();
  }

  createDimensionEditor(container) {
    const editorDiv = container.createDiv({ cls: "dimension-editor" });
    
    // 宽度编辑
    const widthSection = editorDiv.createDiv({ cls: "dimension-section" });
    const widthLabel = widthSection.createEl("label", { text: "宽度 (px)" });
    
    const widthControls = widthSection.createDiv({ cls: "dimension-controls" });
    const widthInput = widthControls.createEl("input", {
      type: "number",
      value: this.cardData.width.toString(),
      attr: { min: "50", max: "2000", step: "10" }
    });
    
    const widthButtons = widthControls.createDiv({ cls: "dimension-buttons" });
    const applyWidthBtn = widthButtons.createEl("button", { text: "应用", cls: "mod-cta mod-small" });
    const resetWidthBtn = widthButtons.createEl("button", { text: "重置", cls: "mod-small" });
    
    // 高度编辑
    const heightSection = editorDiv.createDiv({ cls: "dimension-section" });
    const heightLabel = heightSection.createEl("label", { text: "高度 (px)" });
    
    const heightControls = heightSection.createDiv({ cls: "dimension-controls" });
    const heightInput = heightControls.createEl("input", {
      type: "number", 
      value: this.cardData.height.toString(),
      attr: { min: "50", max: "2000", step: "10" }
    });
    
    const heightButtons = heightControls.createDiv({ cls: "dimension-buttons" });
    const applyHeightBtn = heightButtons.createEl("button", { text: "应用", cls: "mod-cta mod-small" });
    const resetHeightBtn = heightButtons.createEl("button", { text: "重置", cls: "mod-small" });
    
    // 同时应用按钮
    const applyBothDiv = editorDiv.createDiv({ cls: "apply-both-section" });
    const applyBothBtn = applyBothDiv.createEl("button", {
      text: "应用宽度和高度",
      cls: "mod-cta apply-both-btn"
    });

    // 事件处理
    applyWidthBtn.addEventListener("click", async () => {
      const width = parseInt(widthInput.value);
      if (this.validateDimension(width)) {
        await this.updateDimension("width", width);
      }
    });

    applyHeightBtn.addEventListener("click", async () => {
      const height = parseInt(heightInput.value);
      if (this.validateDimension(height)) {
        await this.updateDimension("height", height);
      }
    });

    applyBothBtn.addEventListener("click", async () => {
      const width = parseInt(widthInput.value);
      const height = parseInt(heightInput.value);
      if (this.validateDimension(width) && this.validateDimension(height)) {
        await this.updateBothDimensions(width, height);
      }
    });

    resetWidthBtn.addEventListener("click", () => {
      widthInput.value = this.cardData.width.toString();
    });

    resetHeightBtn.addEventListener("click", () => {
      heightInput.value = this.cardData.height.toString();
    });

    // 键盘支持
    [widthInput, heightInput].forEach(input => {
      input.addEventListener("keypress", (e) => {
        if (e.key === "Enter") {
          applyBothBtn.click();
        }
      });
    });
  }

  async updateDimension(dimension, value) {
    try {
      const newWidth = dimension === "width" ? value : this.cardData.width;
      const newHeight = dimension === "height" ? value : this.cardData.height;
      
      await this.cardService.unifyCardSizes([this.card], { width: newWidth, height: newHeight });
      
      // 更新本地数据和显示
      this.cardData.width = newWidth;
      this.cardData.height = newHeight;
      this.updateSizeDisplay();
      
      new Notice(`卡片${dimension === "width" ? "宽度" : "高度"}已更新为 ${value}px`);
      
    } catch (error) {
      console.error("更新尺寸失败:", error);
      new Notice("更新失败: " + error.message);
    }
  }

  async updateBothDimensions(width, height) {
    try {
      await this.cardService.unifyCardSizes([this.card], { width, height });
      
      this.cardData.width = width;
      this.cardData.height = height;
      this.updateSizeDisplay();
      
      new Notice(`卡片尺寸已更新为 ${width}×${height}px`);
      this.close();
      
    } catch (error) {
      console.error("更新尺寸失败:", error);
      new Notice("更新失败: " + error.message);
    }
  }

  updateSizeDisplay() {
    const sizeEl = this.contentEl.querySelector("#current-size");
    if (sizeEl) {
      sizeEl.textContent = `${this.cardData.width} × ${this.cardData.height} px`;
    }
  }

  validateDimension(value) {
    return !isNaN(value) && value >= 50 && value <= 2000;
  }

  addStyles() {
    const style = document.createElement("style");
    style.textContent = `
      .current-card-info {
        background-color: var(--background-secondary);
        padding: 15px;
        border-radius: 5px;
        margin-bottom: 20px;
      }
      
      .card-summary {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      
      .size-display, .position-display {
        display: flex;
        align-items: center;
        gap: 8px;
      }
      
      .size-label, .pos-label {
        color: var(--text-muted);
        font-size: 0.9em;
      }
      
      .size-value {
        font-weight: bold;
        color: var(--text-accent);
        font-size: 1.1em;
      }
      
      .content-preview {
        margin-bottom: 15px;
        padding: 10px;
        background-color: var(--background-secondary);
        border-radius: 3px;
      }
      
      .preview-label {
        font-size: 0.9em;
        color: var(--text-muted);
        margin-bottom: 5px;
      }
      
      .preview-text {
        font-size: 0.95em;
        line-height: 1.4;
      }
      
      .dimension-editor {
        display: flex;
        flex-direction: column;
        gap: 20px;
      }
      
      .dimension-section {
        padding: 15px;
        border: 1px solid var(--background-modifier-border);
        border-radius: 5px;
      }
      
      .dimension-section label {
        display: block;
        margin-bottom: 10px;
        font-weight: 500;
      }
      
      .dimension-controls {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      
      .dimension-controls input {
        width: 120px;
        padding: 5px 8px;
      }
      
      .dimension-buttons {
        display: flex;
        gap: 5px;
      }
      
      .apply-both-section {
        text-align: center;
        padding-top: 15px;
        border-top: 1px solid var(--background-modifier-border);
      }
      
      .apply-both-btn {
        padding: 10px 30px;
        font-size: 1em;
      }
      
      .mod-small {
        padding: 4px 12px;
        font-size: 0.9em;
      }
    `;
    document.head.appendChild(style);
    
    this.scope.register([], "cleanup-style", () => {
      style.remove();
    });
  }

  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
}


// MultiCardPropertiesModal.ts - 专门用于多卡片批量操作
import { Modal, Notice } from "obsidian";

export class MultiCardPropertiesModal extends Modal {
  constructor(app, cards, cardService) {
    super(app);
    this.cards = cards;
    this.cardService = cardService;
    this.cardInfos = [];
    this.processCardData();
  }

  processCardData() {
    this.cardInfos = this.cards.map((card) => {
      const data = card.getData();
      const textPreview = data.text ? 
        (data.text.length > 50 ? data.text.substring(0, 50) + "..." : data.text) : "";
      return {
        id: data.id,
        width: data.width,
        height: data.height,
        x: data.x,
        y: data.y,
        text: textPreview,
        hasBadge: !!data.badge,
        badgeContent: data.badge
      };
    });

    this.cardInfos.sort((a, b) => {
      if (Math.abs(a.y - b.y) > 10) {
        return a.y - b.y;
      }
      return a.x - b.x;
    });
  }

  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    
    contentEl.createEl("h2", { text: "批量卡片属性管理" });
    
    // 统计信息
    const statsDiv = contentEl.createDiv({ cls: "card-properties-stats" });
    this.createStatisticsSection(statsDiv);
    
    contentEl.createEl("hr");
    
    // 卡片列表
    const listDiv = contentEl.createDiv({ cls: "card-properties-list" });
    this.createCardList(listDiv);
    
    contentEl.createEl("hr");
    
    // 批量操作
    const actionsDiv = contentEl.createDiv({ cls: "card-properties-actions" });
    this.createBatchActions(actionsDiv);
    
    this.addStyles();
  }

  // 保持原有的统计、列表和批量操作方法不变
  createStatisticsSection(container) {
    const stats = this.calculateStatistics();
    const statsContent = container.createDiv({ cls: "stats-content" });
    statsContent.innerHTML = `
      <div class="stat-item">
        <span class="stat-label">选中卡片数量：</span>
        <span class="stat-value">${stats.count}</span>
      </div>
      <div class="stat-item">
        <span class="stat-label">尺寸范围：</span>
        <span class="stat-value">
          宽 ${stats.minWidth} - ${stats.maxWidth} px, 
          高 ${stats.minHeight} - ${stats.maxHeight} px
        </span>
      </div>
      <div class="stat-item">
        <span class="stat-label">平均尺寸：</span>
        <span class="stat-value">${stats.avgWidth} × ${stats.avgHeight} px</span>
      </div>
      <div class="stat-item">
        <span class="stat-label">位置范围：</span>
        <span class="stat-value">
          X: ${stats.minX} - ${stats.maxX}, 
          Y: ${stats.minY} - ${stats.maxY}
        </span>
      </div>
    `;
  }

  // ... 保持其他原有方法不变
}


// 在 main.ts 中的调用逻辑修改
addNodeMenuCommands(menu, node) {
  // ... 其他命令 ...

  // 使用单卡片属性查看器
  if (node.getData && node.getData().type === "text") {
    menu.addSeparator();
    
    menu.addItem((item) => {
      item.setTitle("编辑卡片尺寸...")
        .setIcon("edit")
        .onClick(() => {
          const modal = new SingleCardPropertiesModal(this.app, node, this.cardService);
          modal.open();
        });
    });
  }
}

addSelectionMenuCommands(menu, selection) {
  // ... 其他命令 ...

  // 使用多卡片属性查看器
  const textCards = selectionArray.filter(
    (node) => node.getData && node.getData().type === "text"
  );
  
  if (textCards.length >= 2) {
    menu.addItem((item) => {
      item.setTitle("批量管理卡片属性...")
        .setIcon("settings")
        .onClick(() => {
          const modal = new MultiCardPropertiesModal(this.app, textCards, this.cardService);
          modal.open();
        });
    });
  }
}
```

基于您的反馈，我建议采用功能分离的设计方案，创建两个专门化的属性查看器来分别处理单卡片编辑和多卡片批量管理场景。这种分离将有效消除信息冗余并优化用户体验。

**单卡片编辑器设计重点**：专注于直接编辑功能，移除统计摘要和详细列表等冗余信息展示。界面将仅保留必要的当前尺寸显示和编辑控件，实现最大程度的简洁性和操作效率。

**多卡片管理器设计重点**：保留完整的统计信息、卡片列表和批量操作功能，因为这些信息对于多卡片场景的决策制定具有重要价值。

## 具体实施方案

**SingleCardPropertiesModal 类**将提供精简的编辑界面，包含当前尺寸的简要显示、内容预览（如果存在文本）以及直观的尺寸编辑控件。这个界面将消除所有重复信息，专注于提供高效的单卡片编辑体验。

**MultiCardPropertiesModal 类**将继承现有的完整功能集，包括统计摘要、详细卡片列表和批量操作选项。这种设计确保多卡片管理场景下用户能够获得充分的信息支持来做出批量操作决策。

**菜单集成逻辑**需要相应调整，确保单个节点的右键菜单调用单卡片编辑器，而多选场景下的菜单调用多卡片管理器。这种分离将为不同的使用场景提供最适合的界面和功能。

## 预期效果

实施这一分离方案后，单卡片编辑场景将呈现高度简洁的界面，用户可以直接关注尺寸调整操作而不受冗余信息干扰。多卡片管理场景将保持完整的信息展示和批量操作能力，确保复杂操作场景下的功能完整性。

这种设计方案不仅解决了当前的信息重复问题，还为未来的功能扩展提供了清晰的架构基础。每个Modal类都可以独立优化而不影响另一个使用场景，从而提高代码的可维护性和用户体验的针对性。

建议您按照提供的代码框架实施这一分离方案，这将显著提升插件在不同使用场景下的专业性和易用性。